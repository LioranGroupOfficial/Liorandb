---
title: Production notes (Docker, ops)
sidebar_position: 5
---

## VPS (PM2 + Nginx)

For a full Ubuntu VPS production setup using PM2 + Nginx (rate limiting, TLS, firewall), see:

- [Ubuntu VPS production (PM2 + Nginx)](./ubuntu-vps-pm2-nginx)

## System requirements

Minimum:

- Node.js 18+
- ~512MB RAM
- SSD recommended

## Running in production

Typical flow:

```bash
npm i -g @liorandb/db
NODE_ENV=production PORT=4000 ldb-serve
```

Use `--root` to place data on a dedicated volume/disk.

## Docker example

Use the published Docker image (recommended) and mount a local folder for persistence.

Example `docker run` (Windows PowerShell, one line):

```powershell
docker run -d -p 4000:4000 -v "${PWD}\\liorandb-data:/root/LioranDB" --name liorandb ldep/liorandb:latest
```

This creates (or reuses) a local folder at `.\liorandb-data` next to where you run the command.

Then open Studio:

- `http://localhost:4000`

### User management (inside container)

To manage server users with `ldb-users`, open a shell inside the container:

```bash
docker exec -it liorandb sh
```

If the image has `bash`, you can use `bash` instead of `sh`.

See: [User management (ldb-users)](./users)

Example:

```bash
ldb-users list
```
